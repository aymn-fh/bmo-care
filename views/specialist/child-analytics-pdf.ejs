<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="utf-8" />

  <style>
    <% if (cairoFontRegular && cairoFontBold) { %>
    @font-face {
      font-family: 'Cairo';
      src: url(data:font/ttf;base64,<%= cairoFontRegular %>) format('truetype');
      font-weight: 400;
    }
    @font-face {
      font-family: 'Cairo';
      src: url(data:font/ttf;base64,<%= cairoFontBold %>) format('truetype');
      font-weight: 700;
    }
    <% } %>

    :root {
      --blue: #4f6ef7;
      --pink: #ec3b7c;
      --pink-soft: #fde7ef;
      --orange: #f59e0b;
      --orange-soft: #fff3e0;
      --text: #0f172a;
      --muted: #64748b;
      --border: #e5e7eb;
    }

    * {
      -webkit-print-color-adjust: exact;
      print-color-adjust: exact;
      box-sizing: border-box;
    }

    body {
      margin: 0;
      background: #ffffff;
      font-family: 'Cairo', 'Arial', sans-serif;
      color: var(--text);
      font-size: 11pt;
      line-height: 1.6;
    }

    .page {
      width: 297mm;
      min-height: 210mm;
      padding: 18mm 20mm;
    }

    /* Header */
    .header {
      display: flex;
      justify-content: space-between;
      align-items: flex-end;
      margin-bottom: 4mm;
    }

    .logo {
      font-size: 20pt;
      font-weight: 700;
      color: var(--blue);
    }

    .app-desc {
      font-size: 9.5pt;
      color: var(--muted);
    }

    .line {
      height: 1.2pt;
      background: var(--blue);
      margin-bottom: 6mm;
    }

    /* Report card */
    .report {
      width: 90mm;
      background: linear-gradient(135deg, #f43f5e, #ec4899);
      color: #fff;
      border-radius: 6mm;
      padding: 5mm 6mm;
      margin-bottom: 6mm;
    }

    .report h2 {
      margin: 0 0 3mm;
      font-size: 13pt;
      font-weight: 700;
    }

    .report small {
      display: block;
      font-size: 9pt;
    }

    /* Child card */
    .card {
      background: var(--pink-soft);
      border-radius: 6mm;
      padding: 5mm;
      margin-bottom: 8mm;
    }

    .card-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 4mm;
    }

    .badge {
      background: #fb7185;
      color: #fff;
      padding: 1.8mm 4mm;
      border-radius: 100mm;
      font-size: 8.5pt;
      font-weight: 700;
    }

    .child {
      display: flex;
      align-items: center;
      gap: 3mm;
    }

    .avatar {
      width: 10mm;
      height: 10mm;
      border-radius: 50%;
      background: #ec4899;
      color: #fff;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 9pt;
      font-weight: 700;
    }

    .child-name {
      font-size: 10pt;
      font-weight: 700;
    }

    .child-age {
      font-size: 8.5pt;
      color: var(--muted);
    }

    table {
      width: 100%;
      border-collapse: collapse;
      background: #fff;
      border-radius: 4mm;
      overflow: hidden;
      font-size: 9pt;
    }

    th {
      background: #f8fafc;
      padding: 3mm;
      font-weight: 700;
      color: var(--muted);
      border-bottom: 0.5pt solid var(--border);
    }

    td {
      padding: 3.5mm;
      text-align: center;
      font-weight: 700;
      border-bottom: 0.5pt solid var(--border);
    }

    /* Progress */
    .progress {
      margin-top: 4mm;
      font-size: 8.5pt;
      color: var(--muted);
    }

    .bar {
      height: 2.5mm;
      background: #e5e7eb;
      border-radius: 100mm;
      overflow: hidden;
      margin-top: 1.5mm;
    }

    .bar div {
      height: 100%;
      width: <%= successRate %>%;
      background: #22c55e;
    }

    /* Recommendations */
    .recs {
      border: 0.8pt solid #fbbf24;
      background: var(--orange-soft);
      border-radius: 6mm;
      padding: 5mm;
    }

    .recs h3 {
      margin: 0 0 3mm;
      font-size: 11pt;
      font-weight: 700;
      color: #d97706;
    }

    .recs ul {
      margin: 0;
      padding-right: 5mm;
      font-size: 9.5pt;
      line-height: 1.8;
      font-weight: 600;
      color: #92400e;
    }

    @page {
      size: A4 landscape;
      margin: 0;
    }
  </style>
</head>

<body>
<div class="page">

  <div class="header">
    <div class="app-desc">تطبيق علاج النطق للأطفال</div>
    <div class="logo">نطق</div>
  </div>

  <div class="line"></div>

  <div class="report">
    <h2>تقرير تفصيلي</h2>
    <small>الطفل: <%= childName %></small>
    <small>تاريخ التقرير: <%= reportDate %></small>
  </div>

  <div class="card">
    <div class="card-header">
      <div class="badge"><%= successRate %>% نسبة النجاح</div>
      <div class="child">
        <div>
          <div class="child-name"><%= childName %></div>
          <div class="child-age"><%= childAgeText %></div>
        </div>
        <div class="avatar"><%= childInitial %></div>
      </div>
    </div>

    <table>
      <thead>
        <tr>
          <th>الجلسات</th>
          <th>وقت اللعب</th>
          <th>نسبة النجاح</th>
          <th>متوسط الدرجة</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><%= totalSessions %></td>
          <td><%= playTimeText %></td>
          <td><%= successRate %>%</td>
          <td><%= averageScore %></td>
        </tr>
      </tbody>
    </table>

    <div class="progress">
      التقدم في النطق
      <div class="bar"><div></div></div>
    </div>
  </div>

  <div class="recs">
    <h3>التوصيات</h3>
    <ul>
      <li>الاستمرار في الجلسات اليومية.</li>
      <li>زيادة عدد الجلسات الأسبوعية.</li>
      <li>متابعة التقدم بشكل دوري.</li>
    </ul>
  </div>

</div>
</body>
</html>
