<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="utf-8" />

  <style>
    <% if (cairoFontRegular && cairoFontBold) { %>
    @font-face {
      font-family: 'Cairo';
      src: url(data:font/ttf;base64,<%= cairoFontRegular %>) format('truetype');
      font-weight: 400;
    }
    @font-face {
      font-family: 'Cairo';
      src: url(data:font/ttf;base64,<%= cairoFontBold %>) format('truetype');
      font-weight: 700;
    }
    <% } %>

    :root {
      --blue: #4f6ef7;
      --pink: #ec3b7c;
      --pink-soft: #fde7ef;
      --orange-soft: #fff3e0;
      --text: #0f172a;
      --muted: #64748b;
      --border: #e5e7eb;

      --blue-soft: color-mix(in srgb, var(--blue) 12%, white);
      --primary: <%= (childGender === 'female') ? 'var(--pink)' : 'var(--blue)' %>;
      --primary-soft: <%= (childGender === 'female') ? 'var(--pink-soft)' : 'var(--blue-soft)' %>;
      --primary-light: color-mix(in srgb, var(--primary) 70%, white);
    }

    * {
      box-sizing: border-box;
      -webkit-print-color-adjust: exact;
      print-color-adjust: exact;
    }

    body {
      margin: 0;
      font-family: 'Cairo', Arial, sans-serif;
      font-size: 10.5pt;
      line-height: 1.45;
      color: var(--text);
    }

    .page {
      width: 210mm;
      min-height: 297mm;
      padding: 10mm 12mm;
    }

    /* منع كسر الصفحة داخل العناصر */
    .report, .card, .metric, .box {
      break-inside: avoid;
      page-break-inside: avoid;
    }

    /* Header */
    .header {
      display: flex;
      justify-content: space-between;
      align-items: flex-end;
      margin-bottom: 2.5mm;
    }

    .logo {
      font-size: 20pt;
      font-weight: 700;
      color: var(--primary);
    }

    .app-desc {
      font-size: 9pt;
      color: var(--muted);
    }

    .line {
      height: 1pt;
      background: var(--primary);
      margin-bottom: 3mm;
    }

    /* Report */
    .report {
      background: linear-gradient(135deg, var(--primary), var(--primary-light));
      color: #fff;
      border-radius: 6mm;
      padding: 4mm 5mm;
      margin-bottom: 3mm;
    }

    .report h2 {
      margin: 0 0 2mm;
      font-size: 13pt;
      font-weight: 700;
    }

    .report small {
      font-size: 9pt;
    }

    /* Card */
    .card {
      background: var(--primary-soft);
      border-radius: 6mm;
      padding: 4mm;
      margin-bottom: 4mm;
    }

    .card-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 3mm;
    }

    .badge {
      background: var(--primary);
      color: #fff;
      padding: 1.5mm 4mm;
      border-radius: 100mm;
      font-size: 8.5pt;
      font-weight: 700;
    }

    .child {
      display: flex;
      align-items: center;
      gap: 3mm;
    }

    .avatar {
      width: 9mm;
      height: 9mm;
      border-radius: 50%;
      background: var(--primary);
      color: #fff;
      font-size: 9pt;
      font-weight: 700;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .child-name {
      font-size: 10pt;
      font-weight: 700;
    }

    .child-age {
      font-size: 8.5pt;
      color: var(--muted);
    }

    table {
      width: 100%;
      border-collapse: collapse;
      background: #fff;
      border-radius: 4mm;
      font-size: 9pt;
    }

    th, td {
      padding: 2.6mm;
      text-align: center;
      font-weight: 700;
      border-bottom: 0.5pt solid var(--border);
    }

    th {
      background: #f8fafc;
      color: var(--muted);
    }

    /* Metrics */
    .section-title {
      font-size: 10pt;
      font-weight: 700;
      margin: 3mm 0 2mm;
    }

    .metrics {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 2.5mm;
      margin-bottom: 3mm;
    }

    .metric {
      background: #fff;
      border: 0.5pt solid var(--border);
      border-radius: 4mm;
      padding: 2.8mm;
    }

    .metric .k {
      font-size: 8.5pt;
      color: var(--muted);
      font-weight: 700;
    }

    .metric .v {
      font-size: 10.5pt;
      font-weight: 800;
    }

    .metric .s {
      font-size: 8.5pt;
      color: var(--muted);
    }

    .two-col {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 2.5mm;
      margin-bottom: 0;
    }

    .box {
      background: #fff;
      border: 0.5pt solid var(--border);
      border-radius: 4mm;
      padding: 3.5mm;
    }

    .box h4 {
      margin: 0 0 2mm;
      font-size: 10pt;
      font-weight: 800;
      color: var(--primary);
    }

    .kv {
      display: grid;
      grid-template-columns: 1fr auto;
      gap: 2mm 6mm;
      font-size: 9pt;
      font-weight: 700;
    }

    .kv .val {
      text-align: left;
      direction: ltr;
    }

    /* ===== PAGE 2 ===== */
    .sessions-page {
      break-before: page;
      page-break-before: always;
    }

    .sessions-head {
      display: flex;
      justify-content: space-between;
      align-items: baseline;
      margin: 0 0 3mm;
    }

    .sessions-sub {
      font-size: 8.8pt;
      color: var(--muted);
      font-weight: 700;
    }

    .sessions-table {
      table-layout: fixed;
      font-size: 8.6pt;
    }

    .sessions-table th,
    .sessions-table td {
      padding: 2.1mm;
    }

    .sessions-table th {
      white-space: nowrap;
    }

    .sessions-table tr {
      break-inside: avoid;
      page-break-inside: avoid;
    }

    @page {
      size: A4 portrait;
      margin: 0;
    }
  </style>
</head>

<body>

<!-- PAGE 1 -->
<div class="page">

  <div class="header">
    <div class="app-desc">نراقب بحب، ونحمي بذكاء</div>
    <div class="logo">BMO</div>
  </div>
  <div class="line"></div>

  <div class="report">
    <h2>تقرير تحليلي</h2>
    <small>الطفل: <%= childName %> • الجنس: <%= (typeof childGenderText !== 'undefined' ? childGenderText : '-') %> • تاريخ التقرير: <%= reportDate %></small>
  </div>

  <div class="card">
    <div class="card-header">
      <div class="badge"><%= successRate %>% نسبة النجاح</div>
      <div class="child">
        <div>
          <div class="child-name"><%= childName %></div>
          <div class="child-age"><%= childAgeText %></div>
        </div>
        <div class="avatar"><%= childInitial %></div>
      </div>
    </div>

    <table>
      <tr>
        <th>الجلسات</th>
        <th>وقت اللعب</th>
        <th>نسبة النجاح</th>
        <th>متوسط الدرجة</th>
      </tr>
      <tr>
        <td><%= totalSessions %></td>
        <td><%= playTimeText %></td>
        <td><%= successRate %>%</td>
        <td><%= averageScore %></td>
      </tr>
    </table>

    <div class="section-title">تحليلات إضافية</div>

    <div class="metrics">
      <div class="metric">
        <div class="k">المحاولات (إجمالي)</div>
        <div class="v"><%= totalAttempts || 0 %></div>
        <div class="s">ناجحة: <%= successfulAttempts || 0 %> • فاشلة: <%= failedAttempts || 0 %></div>
      </div>
      <div class="metric">
        <div class="k">متوسط وقت الجلسة</div>
        <div class="v"><%= avgSessionMinutes || 0 %></div>
        <div class="s">دقيقة</div>
      </div>
      <div class="metric">
        <div class="k">آخر 5 جلسات (متوسط)</div>
        <div class="v"><%= (typeof recentSuccessRate !== 'undefined' ? recentSuccessRate : 0) %>%</div>
        <div class="s">متوسط الدرجة: <%= (typeof recentAvgScore !== 'undefined' ? recentAvgScore : 0) %></div>
      </div>
      <div class="metric">
        <div class="k">النشاط</div>
        <div class="v"><%= lastSessionDate || '-' %></div>
        <div class="s">أول نشاط: <%= firstSessionDate || '-' %></div>
      </div>
    </div>

    <div class="two-col">
      <div class="box">
        <h4>أفضل جلسة</h4>
        <% if (bestSession) { %>
        <div class="kv">
          <div>التاريخ</div><div class="val"><%= bestSession.date %></div>
          <div>نسبة النجاح</div><div class="val"><%= bestSession.successRate %>%</div>
          <div>المحاولات</div><div class="val"><%= bestSession.attempts %></div>
          <div>المدة (د)</div><div class="val"><%= bestSession.duration %></div>
        </div>
        <% } else { %>
        <div style="font-size:9pt;color:var(--muted)">لا توجد بيانات</div>
        <% } %>
      </div>

      <div class="box">
        <h4>أقل جلسة</h4>
        <% if (worstSession) { %>
        <div class="kv">
          <div>التاريخ</div><div class="val"><%= worstSession.date %></div>
          <div>نسبة النجاح</div><div class="val"><%= worstSession.successRate %>%</div>
          <div>المحاولات</div><div class="val"><%= worstSession.attempts %></div>
          <div>المدة (د)</div><div class="val"><%= worstSession.duration %></div>
        </div>
        <% } else { %>
        <div style="font-size:9pt;color:var(--muted)">لا توجد بيانات</div>
        <% } %>
      </div>
    </div>
  </div>
</div>

<!-- PAGE 2 -->
<% if (Array.isArray(lastSessions) && lastSessions.length) { %>
<div class="page sessions-page">

  <div class="header">
    <div class="app-desc">نراقب بحب، ونحمي بذكاء</div>
    <div class="logo">BMO</div>
  </div>
  <div class="line"></div>

  <div class="sessions-head">
    <div class="section-title" style="margin:0;">آخر الجلسات</div>
    <div class="sessions-sub">الطفل: <%= childName %> • <%= reportDate %></div>
  </div>

  <table class="sessions-table">
    <thead>
      <tr>
        <th style="width:10%">#</th>
        <th style="width:22%">التاريخ</th>
        <th style="width:16%">المحاولات</th>
        <th style="width:16%">النجاح</th>
        <th style="width:18%">متوسط الدرجة</th>
        <th style="width:18%">المدة (د)</th>
      </tr>
    </thead>
    <tbody>
      <% lastSessions.forEach(function(s, idx) { %>
        <tr>
          <td><%= idx + 1 %></td>
          <td><%= s.date %></td>
          <td><%= s.attempts %></td>
          <td><%= s.successRate %>%</td>
          <td><%= s.averageScore %></td>
          <td><%= s.duration %></td>
        </tr>
      <% }); %>
    </tbody>
  </table>

  <div style="margin-top:8mm; text-align:center; font-size:9pt; font-weight:700; color: var(--muted);">انتهاء الملف</div>

</div>
<% } %>

</body>
</html>
