<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="utf-8" />

  <style>
    <% if (cairoFontRegular && cairoFontBold) { %>
    @font-face {
      font-family: 'Cairo';
      src: url(data:font/ttf;base64,<%= cairoFontRegular %>) format('truetype');
      font-weight: 400;
    }
    @font-face {
      font-family: 'Cairo';
      src: url(data:font/ttf;base64,<%= cairoFontBold %>) format('truetype');
      font-weight: 700;
    }
    <% } %>

    :root {
      --blue: #4f6ef7;
      --pink: #ec3b7c;
      --pink-soft: #fde7ef;
      --orange: #f59e0b;
      --orange-soft: #fff3e0;
      --text: #0f172a;
      --muted: #64748b;
      --border: #e5e7eb;
    }

    * {
      -webkit-print-color-adjust: exact;
      print-color-adjust: exact;
      box-sizing: border-box;
    }

    body {
      margin: 0;
      background: #ffffff;
      font-family: 'Cairo', 'Arial', sans-serif;
      color: var(--text);
      font-size: 11pt;
      line-height: 1.6;
    }

    .page {
      width: 297mm;
      min-height: 210mm;
      padding: 18mm 20mm;
    }

    /* Header */
    .header {
      display: flex;
      justify-content: space-between;
      align-items: flex-end;
      margin-bottom: 4mm;
    }

    .logo {
      font-size: 20pt;
      font-weight: 700;
      color: var(--blue);
    }

    .app-desc {
      font-size: 9.5pt;
      color: var(--muted);
    }

    .line {
      height: 1.2pt;
      background: var(--blue);
      margin-bottom: 6mm;
    }

    /* Report card */
    .report {
      width: 90mm;
      background: linear-gradient(135deg, #f43f5e, #ec4899);
      color: #fff;
      border-radius: 6mm;
      padding: 5mm 6mm;
      margin-bottom: 6mm;
    }

    .report h2 {
      margin: 0 0 3mm;
      font-size: 13pt;
      font-weight: 700;
    }

    .report small {
      display: block;
      font-size: 9pt;
    }

    /* Child card */
    .card {
      background: var(--pink-soft);
      border-radius: 6mm;
      padding: 5mm;
      margin-bottom: 8mm;
    }

    .card-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 4mm;
    }

    .badge {
      background: #fb7185;
      color: #fff;
      padding: 1.8mm 4mm;
      border-radius: 100mm;
      font-size: 8.5pt;
      font-weight: 700;
    }

    .child {
      display: flex;
      align-items: center;
      gap: 3mm;
    }

    .avatar {
      width: 10mm;
      height: 10mm;
      border-radius: 50%;
      background: #ec4899;
      color: #fff;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 9pt;
      font-weight: 700;
    }

    .child-name {
      font-size: 10pt;
      font-weight: 700;
    }

    .child-age {
      font-size: 8.5pt;
      color: var(--muted);
    }

    table {
      width: 100%;
      border-collapse: collapse;
      background: #fff;
      border-radius: 4mm;
      overflow: hidden;
      font-size: 9pt;
    }

    th {
      background: #f8fafc;
      padding: 3mm;
      font-weight: 700;
      color: var(--muted);
      border-bottom: 0.5pt solid var(--border);
    }

    td {
      padding: 3.5mm;
      text-align: center;
      font-weight: 700;
      border-bottom: 0.5pt solid var(--border);
    }

    /* Progress */
    .progress {
      margin-top: 4mm;
      font-size: 8.5pt;
      color: var(--muted);
    }

    .bar {
      height: 2.5mm;
      background: #e5e7eb;
      border-radius: 100mm;
      overflow: hidden;
      margin-top: 1.5mm;
    }

    .bar div {
      height: 100%;
      width: <%= successRate %>%;
      background: #22c55e;
    }

    /* Recommendations */
    .recs {
      border: 0.8pt solid #fbbf24;
      background: var(--orange-soft);
      border-radius: 6mm;
      padding: 5mm;
    }

    .recs h3 {
      margin: 0 0 3mm;
      font-size: 11pt;
      font-weight: 700;
      color: #d97706;
    }

    .recs ul {
      margin: 0;
      padding-right: 5mm;
      font-size: 9.5pt;
      line-height: 1.8;
      font-weight: 600;
      color: #92400e;
    }

    /* Extra analytics */
    .section-title {
      margin: 0 0 3mm;
      font-size: 11pt;
      font-weight: 700;
      color: var(--text);
    }

    .metrics {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 3mm;
      margin-bottom: 6mm;
    }

    .metric {
      background: #ffffff;
      border: 0.5pt solid var(--border);
      border-radius: 4mm;
      padding: 3.5mm;
    }

    .metric .k {
      font-size: 8.5pt;
      color: var(--muted);
      font-weight: 700;
      margin-bottom: 1mm;
    }

    .metric .v {
      font-size: 12pt;
      font-weight: 800;
      color: var(--text);
      line-height: 1.2;
    }

    .metric .s {
      font-size: 8.5pt;
      color: var(--muted);
      font-weight: 700;
      margin-top: 1mm;
    }

    .two-col {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 5mm;
      margin-bottom: 6mm;
    }

    .box {
      background: #ffffff;
      border: 0.5pt solid var(--border);
      border-radius: 4mm;
      padding: 4mm;
    }

    .box h4 {
      margin: 0 0 3mm;
      font-size: 10pt;
      font-weight: 800;
      color: var(--blue);
    }

    .kv {
      display: grid;
      grid-template-columns: 1fr auto;
      gap: 2mm 6mm;
      font-size: 9pt;
      font-weight: 700;
    }

    .kv .key {
      color: var(--muted);
    }

    .kv .val {
      color: var(--text);
      text-align: left;
      direction: ltr;
    }

    .sessions-table td {
      font-weight: 700;
    }

    @page {
      size: A4 landscape;
      margin: 0;
    }
  </style>
</head>

<body>
<div class="page">

  <div class="header">
    <div class="app-desc">تطبيق علاج النطق للأطفال</div>
    <div class="logo">نطق</div>
  </div>

  <div class="line"></div>

  <div class="report">
    <h2>تقرير تفصيلي</h2>
    <small>الطفل: <%= childName %></small>
    <small>تاريخ التقرير: <%= reportDate %></small>
  </div>

  <div class="card">
    <div class="card-header">
      <div class="badge"><%= successRate %>% نسبة النجاح</div>
      <div class="child">
        <div>
          <div class="child-name"><%= childName %></div>
          <div class="child-age"><%= childAgeText %></div>
        </div>
        <div class="avatar"><%= childInitial %></div>
      </div>
    </div>

    <table>
      <thead>
        <tr>
          <th>الجلسات</th>
          <th>وقت اللعب</th>
          <th>نسبة النجاح</th>
          <th>متوسط الدرجة</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><%= totalSessions %></td>
          <td><%= playTimeText %></td>
          <td><%= successRate %>%</td>
          <td><%= averageScore %></td>
        </tr>
      </tbody>
    </table>

    <div style="margin-top:6mm;">
      <div class="section-title">تحليلات إضافية</div>

      <div class="metrics">
        <div class="metric">
          <div class="k">المحاولات (الإجمالي)</div>
          <div class="v"><%= typeof totalAttempts !== 'undefined' ? totalAttempts : 0 %></div>
          <div class="s">ناجحة: <%= typeof successfulAttempts !== 'undefined' ? successfulAttempts : 0 %> • فاشلة: <%= typeof failedAttempts !== 'undefined' ? failedAttempts : 0 %></div>
        </div>

        <div class="metric">
          <div class="k">متوسط وقت الجلسة</div>
          <div class="v"><%= typeof avgSessionMinutes !== 'undefined' ? avgSessionMinutes : 0 %></div>
          <div class="s">دقيقة</div>
        </div>

        <div class="metric">
          <div class="k">آخر 5 جلسات (متوسط)</div>
          <div class="v"><%= typeof recentSuccessRate !== 'undefined' ? recentSuccessRate : 0 %>%</div>
          <div class="s">متوسط الدرجة: <%= typeof recentAvgScore !== 'undefined' ? recentAvgScore : 0 %></div>
        </div>

        <div class="metric">
          <div class="k">آخر نشاط</div>
          <div class="v"><%= lastSessionDate || '-' %></div>
          <div class="s">أول نشاط: <%= firstSessionDate || '-' %></div>
        </div>
      </div>

      <div class="two-col">
        <div class="box">
          <h4>أفضل جلسة</h4>
          <% if (bestSession) { %>
            <div class="kv">
              <div class="key">التاريخ</div><div class="val"><%= bestSession.date %></div>
              <div class="key">نسبة النجاح</div><div class="val"><%= bestSession.successRate %>%</div>
              <div class="key">متوسط الدرجة</div><div class="val"><%= bestSession.averageScore %></div>
            </div>
          <% } else { %>
            <div style="font-size:9pt; color: var(--muted); font-weight:700;">لا توجد بيانات جلسات.</div>
          <% } %>
        </div>

        <div class="box">
          <h4>أقل جلسة</h4>
          <% if (worstSession) { %>
            <div class="kv">
              <div class="key">التاريخ</div><div class="val"><%= worstSession.date %></div>
              <div class="key">نسبة النجاح</div><div class="val"><%= worstSession.successRate %>%</div>
              <div class="key">متوسط الدرجة</div><div class="val"><%= worstSession.averageScore %></div>
            </div>
          <% } else { %>
            <div style="font-size:9pt; color: var(--muted); font-weight:700;">لا توجد بيانات جلسات.</div>
          <% } %>
        </div>
      </div>

      <% if (Array.isArray(lastSessions) && lastSessions.length) { %>
        <div class="section-title" style="margin-top:2mm;">آخر الجلسات</div>
        <table class="sessions-table">
          <thead>
            <tr>
              <th>#</th>
              <th>التاريخ</th>
              <th>المحاولات</th>
              <th>النجاح</th>
              <th>متوسط الدرجة</th>
              <th>المدة (د)</th>
            </tr>
          </thead>
          <tbody>
            <% lastSessions.forEach(function(s) { %>
              <tr>
                <td><%= s.index %></td>
                <td><%= s.date %></td>
                <td><%= s.attempts %></td>
                <td><%= s.successRate %>%</td>
                <td><%= s.averageScore %></td>
                <td><%= s.duration %></td>
              </tr>
            <% }); %>
          </tbody>
        </table>
      <% } %>
    </div>

    <div class="progress">
      التقدم في النطق
      <div class="bar"><div></div></div>
    </div>
  </div>

  <div class="recs">
    <h3>التوصيات</h3>
    <ul>
      <li>الاستمرار في الجلسات اليومية.</li>
      <li>زيادة عدد الجلسات الأسبوعية.</li>
      <li>متابعة التقدم بشكل دوري.</li>
    </ul>
  </div>

</div>
</body>
</html>
